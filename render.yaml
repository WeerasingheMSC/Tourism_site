# render.yaml
staticSites:
  - name: tourism-site-fe
    buildCommand: npm run build
    publishPath: frontend/dist
    redirects:
      - source: /(.*)
        destination: /index.html
        status: 200
